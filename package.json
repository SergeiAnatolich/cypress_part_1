{
  "name": "cypress_part_1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open --browser chrome",
    "cy:open:edge": "npx cypress open --browser edge",
    "cy:open:env": "npx cypress open --config-file cypress.env.config.js --browser edge",
    "cy:open:headless": "npx cypress run --browser chrome"
  },
  "author": "scherbakov",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.4.0"
  }
}
